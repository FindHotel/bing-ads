require "spec_helper"

RSpec.describe Bing::Ads do
  it "has a version number" do
    expect(Bing::Ads::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
